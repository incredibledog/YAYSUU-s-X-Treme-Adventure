/// @description Insert description here
// You can write your code in this editor
if (global.key_jump || global.key_start) && image=limit
{
	obj_fadeblack.fading=true
}
else if (global.key_jump || global.key_start) && image!=limit
{
	image+=1
}
